[build]
builder = "nixpacks"

[deploy]
startCommand = "npm start"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10

[[deploy.beforeStartCommand]]
command = "npx prisma db push --skip-generate && npx prisma db seed"
